# 2026-08-28 硬件移植检测分析记录

## 1. 分析范围

本次分析基于以下本地资料，未修改原 GitHub 工程：

- 原工程：`E:\project_M\src`
- 硬件与参考资料：`E:\project_M\source`
- 旧物料清单：`E:\project_M\source\word(3).xlsx`
- 新开发板原理图：`E:\project_M\source\原理图\STM32F103C8T6D485-1.pdf`
- 新开发板照片：`E:\project_M\source\stm32c8t6.png`
- D24A 四路电机驱动板照片：`E:\project_M\source\D24A.png`
- 编码器电机照片：`E:\project_M\source\motor.png`
- 上位机照片：`E:\project_M\source\上位机.png`

本次移植的基本前提：更换的是开发板，MCU 仍为 STM32F103C8T6，不进行 MCU 架构更换。

## 2. 物料清单分析

旧物料清单更接近旧主控板的采购清单，不是完整的整车 BOM。清单中包含：

- TB6612FNG 电机驱动芯片
- LM2596S-5.0V 降压器件
- MG90S 舵机
- 3S 800mAh 45C 电池和 XT60 接口
- TJA1050 CAN 收发器、120Ω终端电阻
- CH340E、Type-C 接口
- HLK-B0303S-1WR3、B0524S-1WR3 隔离电源模块
- 接插件、紧固件等结构物料

清单没有完整列出四个电机、传感器、上位机及其数量，因此不能仅凭该表还原整车全部接线。

### 2.1 MCU 型号矛盾

旧清单写有 `STM32F103RET6`，但以下工程和资料均指向 `STM32F103C8T6`：

- `E:\project_M\src\c8t6oled.ioc`：`Mcu.CPN=STM32F103C8T6`
- `E:\project_M\source\keil测试工程\NEW.ioc`：`Mcu.CPN=STM32F103C8T6`
- 新开发板文件名、原理图和实物资料均为 C8T6

因此，本次移植统一以 **STM32F103C8T6、LQFP48** 为准。物料清单中的 RET6 视为历史记录或清单错误，不作为引脚设计依据。

### 2.2 旧物料在新方案中的处理

| 旧物料 | 新方案处理 |
|---|---|
| 裸 TB6612FNG | 由 D24A 四路电机驱动板承担 |
| STM32F103 最小系统 | 由新开发板承担 |
| CH340E、Type-C | 不作为默认上位机通信通道 |
| TJA1050、隔离电源、120Ω | 优先使用新板板载 CAN；确认需要隔离 CAN 时再使用 |
| 32.768kHz 晶振 | 使用新开发板现有时钟电路 |
| LM2596S-5.0V | 可继续作为电池到 5V 的主降压 |
| 3S 电池 | 可以保留，但必须确认电机额定电压 |
| MG90S | 可以保留，必须使用稳定的独立 5V 供电 |

## 3. 推荐供电结构

```text
3S 电池（满电约 12.6V）
        │
        ├── 保险丝/总开关 ── D24A VM+
        │
        └── LM2596 5V
                 ├── STM32 开发板 5V 输入
                 ├── MG90S 舵机 5V
                 └── 上位机（仅在确认其供电要求后连接）

非隔离部分的 GND 必须共地
```

注意事项：

1. MG90S 不得从 STM32 的 3.3V 引脚供电。
2. D24A 的 3V3 用于逻辑和编码器供电，不用于驱动电机。
3. D24A 的 `STBY` 第一阶段可直接接 3.3V；如需软件急停，再分配独立 GPIO。
4. 必须确认电机额定电压，不能仅凭 3S 电池信息判断电机能否直接承受满电 12.6V。
5. 电机供电和逻辑/舵机供电应分支布线，避免电机启动电流造成 MCU 复位。

## 4. 原工程资源占用

原工程主要资源如下：

| 功能 | 原工程资源 |
|---|---|
| 上位机串口 | USART1，PA9/PA10，115200bps |
| CAN | PA11/PA12，500kbps |
| WT101 I²C | I2C1 重映射到 PB8/PB9 |
| 四路电机 PWM | TIM2 CH1～CH4，PA0/PA1/PA2/PA3 |
| 左前方向 | PB12/PB11 |
| 左后方向 | PB13/PB14 |
| 右前方向 | PB15/PA8 |
| 右后方向 | PB3/PB4 |
| 两路舵机 PWM | TIM3 CH1/CH2，PA6/PA7 |
| 水泵 | PB0 |
| 超声波 TRIG | PB6 |
| 超声波 ECHO | PB7，TIM4 CH2 输入捕获 |

## 5. 新开发板板载资源

根据新开发板原理图：

- USART1 的 PA9/PA10 接到板载 MAX232 和 DB9 接口。
- 板载 CAN 收发器使用 PB8/PB9。
- 一路板载 RS485 使用 PB10/PB11/PB12/PB13。
- 另一路板载 RS485 使用 PA2/PA3/PA6/PA7。
- MCU 主要引脚同时从排针引出，但板载收发器仍与部分引脚存在电气连接。

因此，引脚从排针引出不代表可以无条件复用。尤其是连接到收发器输出端的 MCU 引脚，不能直接改作推挽输出，否则可能发生电气冲突。

## 6. 已确认的移植冲突

| 功能 | 原引脚 | 冲突 | 建议处理 |
|---|---|---|---|
| CAN | PA11/PA12 | 新板 CAN 固定使用 PB8/PB9 | 将 CAN 重映射到 PB8/PB9 |
| WT101 I²C | PB8/PB9 | 与板载 CAN 重叠 | 将 I2C1 恢复为 PB6/PB7 |
| 电机 PWM C/D | PA2/PA3 | 与板载 RS485 重叠，PA3可能受收发器输出驱动 | 改到其他定时器 PWM 通道 |
| 电机方向 | PB11～PB13 | 与板载 RS485 重叠 | 重新分配为普通空闲 GPIO |
| 舵机 PWM | PA6/PA7 | 与另一组板载 RS485 控制线重叠 | 停用对应 RS485或迁移舵机 PWM |
| 超声波 ECHO | PB7 | 与新的 I²C PB6/PB7 重叠 | 改用其他输入引脚及 EXTI/计时方案 |

## 7. D24A 接线原则

四个电机建议统一编号：

| D24A | 车辆位置 |
|---|---|
| Motor A | 左前 LF |
| Motor B | 左后 LB |
| Motor C | 右前 RF |
| Motor D | 右后 RB |

D24A 每路 6Pin 电机接口包括两根电机线、两根编码器信号线、3V3 和 GND。电机插头方向必须根据 D24A 丝印确认，不能只依赖线色。

控制信号连接原则：

- `PWMA～PWMD`：连接 STM32 的四路定时器 PWM。
- `AIN1/AIN2～DIN1/DIN2`：连接八个方向 GPIO。
- `STBY`：接 3.3V 或独立使能 GPIO。
- `GND`：与 STM32 共地。
- `VM+`：连接电机电源，不连接 STM32 的 5V 或 3.3V。
- `E1A/E1B～E4A/E4B`：编码器信号，第一阶段可以暂不接入闭环控制。

## 8. 编码器代码现状

D24A 可以引出四路电机编码器，但原工程当前并没有完成四轮编码器闭环：

- `Hardwaer/encoder.c` 仅定义左右两路编码器。
- 该文件尝试使用 TIM3 和 TIM4 的编码器模式。
- TIM3 当前已经用于两路舵机 PWM。
- TIM4 当前用于超声波输入捕获。
- 编码器文件未形成与四个电机对应的完整闭环控制链路。

因此，第一阶段推荐保持原程序的实际行为，先完成四电机开环控制、舵机、上位机和传感器移植。四路编码器闭环作为第二阶段单独设计。若四路编码器全部使用双相正交采集，可能需要外部计数器、辅助 MCU，或进一步压缩现有定时器资源。

## 9. 上位机通信注意事项

原工程使用 USART1、115200bps 和固定 10 字节数据包：

- 控制包：`0xB3 ... 0xB4`
- 遥测包：`0xB5 ... 0xB6`

新开发板 USART1 通过 MAX232 接到 DB9，因此默认接口是 RS232 电平。连接上位机前必须确认上位机端接口类型：

- 如果上位机提供 RS232，可直接使用双方 RS232 接口，并核对 TX/RX/GND 和 DB9 公母头定义。
- 如果上位机只提供 3.3V TTL UART，不能直接接新板 DB9，需要改用合适的 TTL 串口通道或电平转换模块。
- 如果改用 CAN，需要在上位机侧具备 CAN 接口和对应软件协议，不能直接沿用现有 UART 数据包链路。

## 10. 推荐移植顺序

1. 确认实物 MCU 丝印为 STM32F103C8T6。
2. 确认电机额定电压、堵转电流和编码器供电电压。
3. 确认上位机使用 RS232、TTL UART、RS485 还是 CAN。
4. 确定需要保留的新开发板板载通信接口。
5. 完成最终 GPIO、PWM、定时器资源分配表。
6. 先验证 STM32 单板启动和调试下载。
7. 验证上位机通信。
8. 在电机悬空状态下逐路测试 D24A 的方向和 PWM。
9. 验证舵机、超声波、水泵和姿态传感器。
10. 最后设计并接入四路编码器闭环。

## 11. 当前结论

1. 原工程和目标资料均以 STM32F103C8T6 为主，本次属于板级移植，不是 MCU 架构迁移。
2. 原有控制算法和通信数据格式可以保留，主要修改集中在 CubeMX/初始化代码、GPIO 定义和定时器通道映射。
3. CAN 必须改到 PB8/PB9；I²C 随之改到 PB6/PB7。
4. 原电机 PWM、方向、舵机和超声波引脚不能整体照搬。
5. 旧清单中的 CH340、裸 TJA1050、最小系统和部分隔离器件已经被新开发板或 D24A 模块替代。
6. 四路编码器闭环不应与第一阶段板级移植同时强行完成。
7. 在上位机接口类型、电机电压和需要保留的 RS485 功能确认前，不应形成最终通电接线图。

## 12. 工程保护状态

- `E:\project_M\src` 保持原样，仅作为移植参考。
- 本次检查未修改原工程源码、配置文件或 Git 历史。
- 检查时 Git 分支为 `main`，工作区无未提交修改。
